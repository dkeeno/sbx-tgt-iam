# =============================================================================
# terraform.tfvars — committed values for sbx-tgt-iam stack
# =============================================================================
# No secrets — all values are public identifiers and naming conventions.

aws_region           = "us-east-1"
github_owner         = "dkeeno"
iam_role_name        = "tgt-github-actions"
iam_role_repo_prefix = ["tgt-"]
